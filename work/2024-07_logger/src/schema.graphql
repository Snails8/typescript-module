type Query {
  hello: String!
  books: [Book!]!
  doError(input: DoErrorInput!): String!
  check: String!
}

type Book {
  title: String!
  author: String!
}

input DoErrorInput {
  params: String
  sub: String
}
